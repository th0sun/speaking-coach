# 🔑 วิธีเปลี่ยน API Key (ง่ายมาก!)

## ✨ นี่คือจุดเด่นของระบบ Modular!

เมื่อก่อน: แก้ในไฟล์ HTML 1,746 บรรทัด 😰  
**ตอนนี้: แก้แค่ 1 ไฟล์เท่านั้น!** 🎉

## 📍 แก้ที่ไหน?

**แก้แค่ไฟล์เดียว:** `backend/.env`

## 🚀 วิธีเปลี่ยน API Key

### ตัวเลือก 1: แก้ด้วย Text Editor (แนะนำ)

```bash
nano backend/.env
```

แก้เป็น:
```bash
GEMINI_API_KEY=your_new_api_key_here
GEMINI_MODEL=gemini-3.0-flash
```

กด `Ctrl+X` → `Y` → `Enter` เพื่อบันทึก

### ตัวเลือก 2: ใช้คำสั่ง (เร็วสุด)

```bash
echo "GEMINI_API_KEY=your_new_api_key_here" > backend/.env
echo "GEMINI_MODEL=gemini-3.0-flash" >> backend/.env
```

### ตัวเลือก 3: เปิดใน VS Code/Editor

```bash
code backend/.env  # หรือ open backend/.env
```

แก้ 2 บรรทัดแล้วบันทึก

## 🔄 Restart Backend

หลังเปลี่ยน API key ต้อง restart backend:

```bash
# 1. หยุด backend เดิม
lsof -ti:5001 | xargs kill -9

# 2. เริ่มใหม่
./start_backend.sh
```

## ✅ เช็คว่าเปลี่ยนสำเร็จ

```bash
curl http://localhost:5001/api/health
```

จะเห็น:
```json
{
  "api_key_preview": "your_new_k...",  ← ต้องเป็น key ใหม่
  "api_key_set": true,
  "model": "gemini-3.0-flash",         ← ต้องเป็น 3.0
  "status": "ok"
}
```

## 📋 สรุป

**ขั้นตอนทั้งหมด:**
1. แก้ `backend/.env` (2 บรรทัด)
2. Restart backend
3. เช็ค `/api/health`
4. เสร็จ! 🎉

**ใช้เวลา: ~30 วินาที**

## 🎯 ข้อดีของระบบใหม่

| เรื่อง | ก่อน Refactor | หลัง Refactor |
|--------|--------------|---------------|
| แก้ API Key | หาใน HTML 1,746 บรรทัด | แก้ `.env` 1 ไฟล์ |
| เช็ค API Key | ดูใน Source code | `curl /api/health` |
| เปลี่ยน Model | หา hardcode ในโค้ด | แก้ `.env` บรรทัดเดียว |
| เวลาที่ใช้ | 5-10 นาที | 30 วินาที |

## 💡 Tips

- **ไม่ต้องแก้โค้ด** - แค่แก้ `.env`
- **Git ignore** - `.env` ถูก gitignore อยู่แล้ว (ปลอดภัย)
- **ทดสอบง่าย** - ใช้ `/api/health` เช็คได้ทันที
- **Rollback ง่าย** - ใส่ API key เก่ากลับก็ได้

## 🔐 ความปลอดภัย

✅ API key ไม่มีใน frontend  
✅ API key ไม่มีใน Git  
✅ API key อยู่แค่ใน backend/.env  
✅ แก้ได้ง่ายโดยไม่ต้องแตะโค้ด
