# 🚀 คู่มือการนำ Speaking Coach ขึ้น Cloud (ใช้ได้ทุกที่ทั่วโลก)

เพื่อให้สามารถใช้งาน Speaking Coach ได้บนทุกอุปกรณ์ (iPad, มือถือ, คอมเครื่องอื่น) โดยไม่ต้องเปิดคอมทิ้งไว้ เราจะนำโปรเจกต์ขึ้น Cloud ฟรี ดังนี้:

## 🏗️ โครงสร้าง
*   **Backend (Python/Flask):** ฝากไว้ที่ **Render.com** (ฟรี)
*   **Frontend (HTML/JS):** ฝากไว้ที่ **GitHub Pages** (ฟรี)

---

## ส่วนที่ 1: นำ Backend ขึ้น Render ☁️

1.  **สมัครสมาชิก:** ไปที่ [render.com](https://render.com) และ Login (แนะนำใช้ GitHub Account)
2.  **เตรียม GitHub Repo:**
    *   นำโค้ดทั้งหมดขึ้น GitHub Repository ส่วนตัวของคุณ (ถ้ายังไม่มี)
3.  **สร้าง Web Service:**
    *   ใน Render Dashboard กด **"New +"** -> **"Web Service"**
    *   เลือก Repository ที่เราเพิ่งอัปโหลด
4.  **ตั้งค่า (Settings):**
    *   **Name:** `speaking-coach-backend` (หรือชื่ออื่นที่ชอบ)
    *   **Region:** Singapore (ใกล้ไทยสุด)
    *   **Runtime:** Python 3
    *   **Build Command:** `pip install -r backend/requirements.txt`
        *   *หมายเหตุ:* ระบบอาจตรวจเจอเป็น `pip install -r requirements.txt` ให้แก้ path ให้ถูกเป็น `backend/requirements.txt` ถ้าไฟล์อยู่ในโฟลเดอร์ backend
    *   **Start Command:** `cd backend && gunicorn server:app`
        *   *สำคัญ:* ต้องมี `cd backend` นำหน้าเพื่อให้หาไฟล์ `server.py` เจอ
5.  **ตั้งค่า Environment Variables (Environment Tab):**
    *   กด **"Add Environment Variable"**
    *   **Key:** `GEMINI_API_KEY`
    *   **Value:** (ใส่ API Key ของคุณที่ก๊อปมาจาก Google AI Studio)
    *   **Key:** `GEMINI_MODEL`
    *   **Value:** `gemini-2.0-flash` (หรือรุ่นที่ต้องการ)
    *   **Key:** `PYTHON_VERSION` (Optional)
    *   **Value:** `3.10.0`
6.  **Deploy:** กด **"Create Web Service"**
    *   รอสักพัก (1-2 นาที) จนขึ้นสถานะ **Live**
    *   Copy URL ด้านบนซ้ายมาเก็บไว้ (เช่น `https://speaking-coach-backend.onrender.com`)

---

## ส่วนที่ 2: ตั้งค่า Frontend ให้เชื่อมต่อ Backend ใหม่ 🔗

1.  กลับมาที่ไฟล์ `js/config.js` ในเครื่องของคุณ
2.  แก้บรรทัด `BACKEND_URL` ให้เป็น URL ที่ได้จาก Render:
    ```javascript
    const CONFIG = {
        // BACKEND_URL: 'http://localhost:5001', // อันเก่า (Local)
        BACKEND_URL: 'https://speaking-coach-backend.onrender.com', // อันใหม่ (Cloud) <-- เปลี่ยนเป็นอันนี้
        // ...
    };
    ```
3.  Commit และ Push โค้ดขึ้น GitHub อีกครั้ง

---

## ส่วนที่ 3: นำ Frontend ขึ้น GitHub Pages 🌐

1.  ไปที่ GitHub Repository ของคุณ
2.  ไปที่แท็บ **Settings** -> เมนูซ้ายเลือก **Pages**
3.  **Build and deployment:**
    *   **Source:** Deploy from a branch
    *   **Branch:** `main` (หรือ branch ที่คุณใช้) -> `/root` (หรือ `/docs` ถ้าคุณย้ายไฟล์)
4.  กด **Save**
5.  รอสักครู่ (ประมาณ 1 นาที) GitHub จะแจ้งว่า deploy เสร็จแล้ว พร้อมให้ URL (เช่น `https://yourusername.github.io/speaking-coach/`)

---

## 🎉 เสร็จสมบูรณ์!
ตอนนี้คุณสามารถส่ง Link GitHub Pages ให้เพื่อน หรือเปิดบน iPad / iPhone / Android ได้ทันที โดยระบบจะทำงานเชื่อมกับ Backend บน Render อัตโนมัติ

**ข้อควรระวัง:**
*   **Render Free Tier:** หากไม่มีคนใช้สักพัก Server จะหลับ (Spin down) การเรียกใช้ครั้งแรกอาจรอ 30-50 วินาที ให้ Server ตื่น หลังจากนั้นจะเร็วปกติ
